// ! #include - preprocessor directive(before running the code it includes iostream header file/library)
#include <iostream>     // * iostream - this is header file/library allows user to take input and output
using namespace std;  // * using namespace - used to use the standard library in our code

//👇🏻 return type
int main(){     // * Entery point
    long x , y;
    cout << x << '\n';
    cout << y << '\n';

    int a = 1.1 * 10;
    cout << a << '\n';
    cout << "Hello, World!";
    return 0;   // * use to indicate the end of the program / successfull completion
}

// ! cout -> character output / << - insertion/put to operator
// ! cin -> character input / >> - exertation operator