#include <iostream>
using namespace std;


int main(){

    //! check whether the entered year is a leap year or not
    // int year;
    // cin >> year;

    // if(year % 4 == 0 && (year % 100 != 0 || year % 400 == 0)){
    //     cout << "it's a leap year" << year << endl;
    // }
    // else{
    //     cout << "it's not a leap year" << year << endl;
    // }


    //! sum of n numbers
    // int n;
    // cin >> n;
    // cout << "Sum of n numbers is : " << (n*(n+1)/2) << endl;

    //* Conversion from decimal to binary
    int n = (1%2);
    cout << n << endl;
    
    return 0;
}